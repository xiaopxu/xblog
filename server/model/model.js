const User = require('./all_user')
const Article = require('./article')
const LoginRemember = require('./login_remenber')

const Models = {
    User: User,
    Article: Article,
    LoginRemember: LoginRemember
}

module.exports = Models
